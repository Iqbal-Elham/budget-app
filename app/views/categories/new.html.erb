<%= render 'shared/header', title: "New Category" %>

<%= form_with(model: @category) do |form| %>
  <% if @category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
        <% @category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-fields m-1">
    <div>
    <%= form.text_field :name, class: "form-field", placeholder: "Name" %>
  </div>

  <div>
    <%= form.text_field :icon, class: "form-field", placeholder: "Icon (URL)" %>
  </div>

  <div>
    <%= form.submit class: "btn btn-primary w-100 mt-2" %>
  </div>
  </div>
<% end %>


